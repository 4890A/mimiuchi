/**
 * Styling for a tag chip on a library card.
 *
 * Shared by the card's own row and by the overflow popup so the two cannot
 * drift apart — the popup is meant to read as the row continuing, not as a
 * separate menu.
 */
export const TAG_CHIP_CLASS =
  "rounded border border-border/60 px-1.5 py-0.5 text-[10px] text-muted-foreground transition-colors hover:border-primary/40 hover:text-foreground";
