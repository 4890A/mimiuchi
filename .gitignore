# dependencies
node_modules/
.pnp
.pnp.*

# next.js
.next/
.next-e2e/
out/
next-env.d.ts

# build
build/
dist/
*.tsbuildinfo

# env
.env
.env.local
.env.*.local

# local data (SQLite + downloaded media)
data/*.db
data/*.db-shm
data/*.db-wal
data/*.sqlite
data/*.sqlite-*
covers/

# playwright artifacts
.playwright/
.playwright-cli/
playwright-report/
test-results/
# Kept even though web/e2e/ is ignored below: .auth/state.json holds a signed
# session cookie and must stay out of the repo if the suite is ever tracked.
web/e2e/.auth/

# browser automation suite (kept local, not committed)
web/e2e/
web/playwright.config.ts

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# claude / editor / OS
.claude/
.mcp.json
AGENTS.md
**/AGENTS.md
**/CLAUDE.md
.vscode/
.idea/
.DS_Store
Thumbs.db

# vercel
.vercel
